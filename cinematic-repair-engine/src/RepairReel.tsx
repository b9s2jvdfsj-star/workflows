import { AbsoluteFill, Sequence, Video, Audio, staticFile } from "remotion"; export const RepairReel: React.FC<{videoSrc: string; audioAmbience: string; audioEffects: string; finalSnip: string;}> = ({ videoSrc, audioAmbience, audioEffects, finalSnip }) => { return ( <AbsoluteFill style={{ backgroundColor: "black" }}> <Video src={staticFile(videoSrc)} style={{ width: "100%", height: "100%", objectFit: "cover", filter: "contrast(1.2) brightness(0.9) saturate(0.8)" }} /> <Audio src={staticFile(audioAmbience)} loop /> <Sequence from={60}> <Audio src={staticFile(audioEffects)} /> </Sequence> <Sequence from={390}> <Audio src={staticFile(finalSnip)} /> </Sequence> </AbsoluteFill> ); };
